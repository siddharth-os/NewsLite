<%-include('partials/header')-%>
<!-- Navbar -->
<%-include('partials/navbar')-%>
<!-- MAin Content -->
<div class="container-fluid">
  <div class="row">
    <div class="col-9 main-container">
      <hr>
      <h1>Results for "<%=headings%>"</h1>
      <hr>
      <%  for(var i=0;i<20;i++){%>
      <div class="news-main">
        <img class="main-img" src=<%= news.articles[i].urlToImage%> alt="not available">
        <h4><%=news.articles[i].title%></h4>
        <p><%=news.articles[i].content%>...<a href=<%=news.articles[i].url%>>Read Full Article</a></p>
      </div>
      <hr />
      <%}%>
      <button class="btn btn-primary btn-md show-btn btn-block" name="button">Show More</button>
    </div>
    <%-include("partials/aside")-%>
  </div>
  </div>
  <%-include('partials/contact')-%>
<%-include('partials/footer')-%>
